apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: ratings-request-timeout
spec:
  hosts:
    - ratings
  http:
    - route:
        - destination:
            host: ratings
            subset: v2
      timeout: 2.5s